<?xml version="1.0" encoding="utf-8"?>
<resources>
    <dimen name="map_bounds_padding">96dp</dimen>
    <!-- Aire extra bajo cámara / recorte además de los insets del sistema -->
    <dimen name="top_content_safe_extra">16dp</dimen>
    <!-- Barra inferior: algo más compacta; padding V simétrico para centrar iconos -->
    <dimen name="bottom_nav_min_height">60dp</dimen>
    <dimen name="bottom_nav_item_padding_vertical">5dp</dimen>
    <!-- Separación entre el panel de filtros flotante y la primera tarjeta del listado -->
    <dimen name="list_filters_recycler_gap">8dp</dimen>
    <!-- 0.55 = el panel se mueve al 55% del gesto (más lento que 1:1). Subir = más rápido, bajar = más lento. -->
    <item name="list_filters_header_scroll_damping" format="float" type="dimen">0.55</item>
</resources>
